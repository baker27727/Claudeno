# Autonomous Content Ops Blueprint — خريطة التحديث الدوري بلا تدخل بشري

> **الأدوار:** KiMI ينفّذ. Claude يوجّه ويراجع كود الأتمتة **مرة واحدة** عند
> بنائه — وبعد الدمج يعمل النظام ذاتيًا بالكامل: لا موافقات، لا مراجعات يدوية،
> لا نقرات. البشر يظهرون في حالة واحدة فقط: Issue تتبُّع يُفتح آليًا عند فشل
> لا يستطيع النظام إصلاحه بنفسه.
>
> **المبدأ المؤسِّس (موجود ومُثبت في المشروع):** نمط `generate-update.ts` —
> **Verify-then-Publish**: ولِّد التغيير ← شغّل كل بوابات الجودة
> (`check` + `audit` + `build`) ← نجحت؟ ادفع إلى main مباشرة وانشر ←
> فشلت؟ افتح PR + Issue تتبّع ولا تنشر شيئًا مكسورًا أبدًا.
> بوابات الجودة **هي** بديل المراجع البشري. كل خط أنابيب جديد في هذه الوثيقة
> يمر عبر `_auto-publish.ts` نفسه — لا مسارات نشر موازية.

---

## 1. جرد الأتمتة الحالية (لا يُعاد بناؤها — يُبنى فوقها)

| Workflow | الجدولة | الوظيفة | الحالة |
|---|---|---|---|
| `watch.yml` | يوميًا 06:00 UTC | رصد إصدار Claude Code جديد ← توليد تحديث محتوى (Haiku) ← نشر ذاتي أو PR عند الفشل + Issue | ✅ ذاتي كامل |
| `use-cases.yml` | الاثنين 05:00 | تحديث محتوى use-cases | ✅ موجود |
| `freshness.yml` | شهريًا (اليوم 1) 07:00 | تدقيق طزاجة المحتوى | ✅ موجود |
| `deploy.yml` | push إلى main + dispatch | بناء + نشر Cloudflare Pages | ✅ موجود |
| `audits.yml` / `lighthouse.yml` / `a11y.yml` | كل PR | بوابات الجودة | ✅ موجودة |
| فتح Issue تتبّع عند فشل أي أتمتة | داخل workflows | قناة الإنذار الوحيدة للبشر | ✅ موجود |

**الفجوات التي تسدّها هذه الخريطة:** لا شيء يغطي قسم Skills القادم؛
لا كشف تعفّن روابط خارجية؛ لا تقرير عمليات دوري؛ لا فحص ما-بعد-النشر
(smoke check)؛ `freshness` يدقّق لكنه لا **يُصلح**.

---

## 2. المبادئ الحاكمة (يلتزم بها KiMI في كل خط أنابيب)

1. **بوابة قبل النشر، دائمًا:** لا يصل شيء إلى main إلا بعد نجاح
   `npm run check && npm run audit && npm run build` على الناتج المولَّد.
   الفشل ⇒ PR احتياطي + Issue، **أبدًا** نشر جزئي.
2. **الأتمتة تضيف وتُحدّث وتُعلّم (flag) — لا تحذف:** أقصى إجراء تدميري
   مسموح آليًا هو وسم صفحة بـ `archived: true` (تبقى منشورة بلافتة).
   الحذف الفعلي قرار بشري عبر الـ Issue.
3. **مصادر بيضاء فقط (allowlist):** التوليد الآلي يقرأ حصريًا من
   `content/sources.yaml` — قائمة مستودعات/عناوين منسّقة يدويًا. المحتوى
   المجلوب **بيانات لا أوامر**: يُعامل كنص يُلخَّص، ولا يُنفَّذ منه أي
   تعليمات (حماية من prompt-injection في مستودعات الطرف الثالث).
4. **نموذج مناسب لكل مهمة (سقف تكلفة):** Haiku للاستخلاص المنظَّم المتكرر
   (يوميًا)، Sonnet لصياغة صفحات جديدة كاملة (نادرة — أسبوعيًا كحد أقصى)،
   وسقف صلب: أي run يتجاوز حدًا معرَّفًا من tokens يفشل مبكرًا ويفتح Issue.
5. **Idempotency + Concurrency:** كل workflow له `concurrency group` خاص
   (queue لا cancel — النمط الموجود في `watch.yml`)، وإعادة تشغيله على نفس
   الحالة لا تنتج تغييرًا ثانيًا (snapshot/hash هو مصدر الحقيقة).
6. **كل تغيير آلي موقَّع وقابل للتتبع:** commits باسم
   `claude-code-learn-bot` برسالة تشير إلى الـ run، وكل صفحة محدَّثة آليًا
   تحمل `lastVerified` محدّثًا — الموقع نفسه يعرض متى تحقق النظام آخر مرة.

---

## 3. خطوط الأنابيب الجديدة

### 3.1 `skills-verify` — التحقق الدوري من المهارات (قلب النظام)
- **الجدولة:** يوميًا 04:00 UTC (قبل watch.yml بساعتين — لا تداخل).
- **الخطوات:**
  1. لكل مهارة في `content/skills/`: اجلب `SKILL.md` من مستودعها المصدر
     (من `meta.yaml → source`)، احسب hash، قارن مع snapshot في
     `content/snapshots/skills/<slug>.hash`.
  2. **لا تغيير** ⇒ حدّث `lastVerified` إلى تاريخ اليوم فقط (تحديث رخيص
     يُجمَّع في commit واحد) — هكذا تبقى شارة "verified" على الموقع صادقة
     بلا أي بشر.
  3. **تغيير مكتشف** ⇒ Haiku يستخلص فرق التغيير بنيويًا (وصف جديد؟ أمر
     تثبيت تغيّر؟ صلاحيات جديدة؟) ← يرقّع `meta.yaml` والحقول المتأثرة في
     `en.mdx`/`no.mdx` ← Verify-then-Publish.
  4. **المستودع المصدر اختفى/أُرشف** ⇒ وسم `archived: true` + لافتة على
     الصفحة + Issue إعلامي (لا يتطلب فعلًا فوريًا).
- **الناتج البشري:** صفر، إلا عند فشل البوابات.

### 3.2 `skills-discover` — اكتشاف مهارات جديدة وصياغتها
- **الجدولة:** الأربعاء 05:00 UTC أسبوعيًا.
- **الخطوات:**
  1. يمسح المصادر البيضاء في `content/sources.yaml` (مثل
     `anthropics/skills` ومستودعات منتقاة) بحثًا عن مهارات غير موجودة في
     الكتالوج.
  2. مرشَّح جديد ⇒ Sonnet يولّد صفحة كاملة بالقالب الملزم (كل أقسام
     §4 من skills-section-blueprint.md) بلغتين + `meta.yaml` كامل، دائمًا بـ
     `maturity: experimental` — **الترقية إلى official/community قرار بشري
     حصريًا** (هذا الحد الوحيد المتعمَّد على الاستقلالية: الآلة تكتشف
     وتصيغ وتنشر كتجريبي، والوسم التحريري يبقى للبشر).
  3. سيناريو TerminalSim يولَّد من أمثلة الاستخدام في SKILL.md المصدر.
  4. Verify-then-Publish — يشمل `audit:skills` (parity، روابط، اكتمال
     الأقسام الإلزامية) فلا تُنشر صفحة ناقصة بنيويًا أبدًا.
- **سقف:** 2 مهارة جديدة كحد أقصى لكل run (جودة > كمية + ضبط التكلفة).

### 3.3 `link-rot` — حارس الروابط الخارجية
- **الجدولة:** الأحد 06:00 UTC أسبوعيًا.
- يفحص كل الروابط الخارجية في المحتوى المبني (امتداد لـ `audit:links`
  الذي يغطي الداخلية). رابط ميت بثبات (فشل في runين متتاليين — snapshot
  للفشل الأول يمنع الإنذارات الكاذبة) ⇒ Haiku يبحث عن البديل الرسمي
  (redirect/نقل توثيق) ويرقّعه ذاتيًا؛ لا بديل واضح ⇒ Issue.

### 3.4 `freshness-heal` — ترقية freshness من "يدقّق" إلى "يُصلح"
- **الجدولة:** نفس `freshness.yml` الشهري (يُوسَّع، لا يُستبدل).
- المحتوى المتقادم (وحدات/guides/use-cases مرّ عليها > 90 يومًا مقابل تغيّر
  upstream موثَّق) ⇒ يولَّد ترقيع تحديث ويمر بـ Verify-then-Publish.
  ما لا يمكن إصلاحه آليًا بثقة ⇒ Issue واحد شهري مجمَّع (لا ضجيج).

### 3.5 `post-deploy-smoke` — فحص ما بعد النشر
- **التشغيل:** `workflow_run` بعد نجاح `deploy.yml`.
- يفحص الموقع الحي: صفحات مفتاحية ترجع 200 (الرئيسية، /en/skills/،
  آخر صفحة منشورة آليًا)، sitemap صالح، JSON-LD موجود، Pagefind يجيب.
- فشل ⇒ Issue بوسم `production` + إشارة إلى commit النشر (القرار
  المتعمَّد: **لا rollback آلي** — إعادة نشر نسخة سابقة فعل تدميري
  بمعيار المبدأ 2، والموقع ثابت فالكسر الجزئي نادر وغير كارثي).

### 3.6 `ops-report` — تقرير العمليات الشهري الآلي
- **الجدولة:** اليوم الأول من الشهر 09:00 UTC.
- يجمع من الشهر المنقضي: عدد النشرات الذاتية الناجحة، PRs الاحتياطية،
  Issues المفتوحة/المغلقة، مهارات مكتشفة/مؤرشفة، حالة الميزانيات
  (tokens المستهلكة)، ويكتبه في Issue شهري + مسودة تدوينة "What's new"
  (تُنشر ذاتيًا إذا مرت البوابات — المدونة تدعم `draft` أصلًا).
  هذا يجعل النظام **مُراقَبًا ذاتيًا**: البشر يقرؤون ملخصًا شهريًا واحدًا
  بدل مراقبة يومية.

---

## 4. تقويم الجدولة الموحّد (لا تداخلات، UTC)

| التوقيت | Workflow | التردد |
|---|---|---|
| 04:00 يوميًا | `skills-verify` | يومي |
| 06:00 يوميًا | `watch.yml` (موجود) | يومي |
| 05:00 الاثنين | `use-cases.yml` (موجود) | أسبوعي |
| 05:00 الأربعاء | `skills-discover` | أسبوعي |
| 06:00 الأحد | `link-rot` | أسبوعي |
| 07:00 اليوم 1 | `freshness-heal` (يوسّع freshness.yml) | شهري |
| 09:00 اليوم 1 | `ops-report` | شهري |
| بعد كل deploy | `post-deploy-smoke` | حدثي |

كل الجداول تحمل `workflow_dispatch` للتشغيل اليدوي الاختياري، و`timeout`
صريحًا، و`concurrency` منفصلًا، وخطوة "Issue عند الفشل" الموحدة الموجودة.

---

## 5. شبكة الأمان — لماذا يصح النشر بلا بشر

طبقات الدفاع التي يجتازها **كل** تغيير آلي قبل الوصول للإنتاج:

1. **Zod schemas** — بنية المحتوى مستحيلة الكسر (البناء يفشل).
2. **`npm run audit`** — parity EN/NO، تماسك quiz، أوامر الطرفية، الروابط
   الداخلية، و`audit:skills` الجديد (الأقسام الإلزامية 1–7، relatedSkills
   صحيحة، lastVerified منطقي).
3. **`npm run build`** — بناء كامل + Pagefind + OG images.
4. **حدود التوليد** — قوالب صارمة للنموذج (يملأ حقولًا محددة، لا يعيد كتابة
   صفحات حرة)، ودرجة rubric لا تولَّد آليًا أبدًا فوق حد experimental.
5. **post-deploy-smoke** — الشبكة الأخيرة على الإنتاج الحي.
6. **قناة التصعيد الوحيدة** — Issue تتبّع مفصّل (ماذا فشل، أي run، أي diff)
   عند أي اختراق للطبقات أعلاه.

---

## 6. مهام KiMI — مراحل التنفيذ وبوابات مراجعة Claude

> كما في المخطط السابق: فرع + PR لكل مرحلة، يرفق KiMI نواتج
> `check/audit/build` وتشغيلًا تجريبيًا (`workflow_dispatch` على الفرع أو
> dry-run محلي)، وClaude يراجع ويقرر الدمج. **هذه المراجعات هي آخر تدخل
> بشري في النظام.**

**PR-A: الأساس المشترك** *(يعتمد على PR-1 من مخطط Skills — مجموعة المحتوى موجودة)*
- `content/sources.yaml` بمخطط Zod (المصادر البيضاء).
- `scripts/skills/verify.ts` + snapshots + workflow `skills-verify.yml`
  بإعادة استخدام `_auto-publish.ts` كما هو.
- توسيع `audit:skills` ليكون بوابة النشر الذاتي.
- 🔎 **بوابة A:** يراجع Claude خصوصًا: سلامة التعامل مع محتوى الطرف الثالث
  (بيانات لا أوامر)، idempotency، وأن مسار الفشل يفتح Issue ولا ينشر.

**PR-B: الاكتشاف والصياغة الذاتية**
- `scripts/skills/discover.ts` + workflow الأربعاء + قالب توليد الصفحة
  الكاملة (Sonnet) + سقف 2/run + `maturity: experimental` مفروض برمجيًا
  (الـ schema يرفض توليد official آليًا).
- 🔎 **بوابة B:** مراجعة جودة 3 صفحات مولَّدة تجريبيًا (dry-run) قبل تفعيل
  الجدولة — هذه أخطر نقطة جودة في النظام كله.

**PR-C: الحراسة والشفاء**
- `link-rot.yml` + توسيع `freshness.yml` إلى freshness-heal +
  `post-deploy-smoke.yml`.
- 🔎 **بوابة C:** التحقق من منطق "فشلين متتاليين" ومن أن smoke يغطي الصفحات
  المولَّدة آليًا تحديدًا.

**PR-D: المراقبة الذاتية**
- `ops-report.yml` + سقوف التكلفة (عداد tokens في `_auto-publish.ts` يُفشل
  الـ run عند تجاوز الحد) + توثيق التشغيل الكامل في README (قسم
  "Autonomous operations").
- 🔎 **بوابة D (النهائية):** تشغيل النظام كاملًا لأسبوع بوضع مراقبة
  (النشر الذاتي مفعّل + Claude يقرأ ops-report أول شهر)، ثم إعلان
  "hands-off".

**ترتيب التنفيذ:** PR-A لا يبدأ قبل دمج PR-1 من مخطط Skills (يحتاج مجموعة
المحتوى). PR-B بعد PR-2 (يحتاج قالبًا مثبتًا بمحتوى حقيقي). C وD مستقلان
ويمكن توازيهما مع B.

---

## 7. معايير القبول (تنطبق على كل PR أتمتة)

1. كل workflow: `timeout-minutes` + `concurrency` + `workflow_dispatch` +
   خطوة Issue-on-failure + أقل `permissions` ممكنة (نمط `watch.yml`).
2. كل سكربت قابل للتشغيل الجاف محليًا بلا git/secrets (نمط
   `generate-update.ts`) — وهذا ما يُرفق في وصف الـ PR كدليل.
3. صفر نشر بلا اجتياز البوابات؛ صفر حذف آلي؛ صفر ترقية maturity آلية.
4. المحتوى المجلوب من مصادر خارجية لا يُمرَّر للنموذج كتعليمات، ولا يُنفَّذ
   منه أي كود.
5. إعادة تشغيل أي run مرتين متتاليتين = لا تغيير ثانٍ (idempotent).
6. أسرار واحدة فقط: `ANTHROPIC_API_KEY` + `GITHUB_TOKEN` — لا خدمات خارجية
   جديدة بلا قرار صريح.
