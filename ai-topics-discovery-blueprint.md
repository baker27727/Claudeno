# AI Topics Discovery Blueprint — من قائمة ثابتة إلى اكتشاف ذكي للمواضيع

> **الأدوار:** KiMI ينفّذ، Claude يخطط ويراجع (كما في [[kimi-executor-claude-reviewer]]).
> هذه الوثيقة ترد على فحص كشف أن `/no/fagomrader/` (use-cases) تعمل على **قائمة ثابتة
> من 9 مهن مكتوبة يدويًا في الكود** (`scripts/use-cases/sources.ts`) — تُحدَّث
> أسبوعيًا لكن **لا تكتشف مواضيع جديدة أبدًا**. هذه الخطة تبني طبقة اكتشاف
> واختيار ذكية فوق النمط الآمن الموجود (Verify-then-Publish)، تماثل
> `skills-discover` في الروح لكنها مصمَّمة لموضوع مختلف تمامًا: ليست "مهارة
> برمجية موجودة في مستودع" بل **"موضوع ناشئ عن Claude/الذكاء الاصطناعي يستحق
> مقالًا"** — واكتشافها يحتاج قياس اتجاه (trend) لا مجرد جلب ملف.

---

## 1. المشكلة بالضبط (من فحص الكود، لا افتراضًا)

```
scripts/use-cases/sources.ts → USE_CASE_SOURCES = { 9 مفاتيح ثابتة }
scripts/use-cases/research.ts → for (profession of Object.keys(USE_CASE_SOURCES))
scripts/use-cases/generate.ts → for ([profession, sourceSet] of Object.entries(USE_CASE_SOURCES))
```

لا حلقة تكتشف مفتاحًا عاشرًا أبدًا. الأتمتة الأسبوعية (`use-cases.yml`، الاثنين
05:00 UTC) تعيد فحص **نفس الروابط** لنفس **9 مهن** إلى الأبد. هذا مقبول
كصفحات مهنة ثابتة عالية القيمة لـ SEO (تبقى)، لكنه **لا يجيب سؤال المستخدم**:
"ما الجديد في Claude/AI الذي يستحق أن نكتب عنه، وما مدى قوة الحاجة إليه؟"

## 2. القرار المعماري: طبقة جديدة موازية، لا استبدال

لن نمس التسع صفحات المهنية — هي ركائز SEO مستقرة وتخدم جمهورًا محددًا
بالبحث الوظيفي ("Claude for marketing" إلخ). النمطان مختلفان في دورة الحياة:

| | صفحات المهن (موجودة) | مقالات الاتجاهات (جديدة) |
|---|---|---|
| عدد الصفحات | 9 ثابتة، تُحدَّث | ينمو أسبوعيًا (بحد أقصى) |
| المحفّز | مراجعة دورية لمصادر ثابتة | اكتشاف + تسجيل نقاط اتجاه |
| القيمة | مرجعية دائمة حسب الوظيفة | حداثة وسبق ("ماذا يحدث الآن") |
| العمق | متوسط، عملي | مزدوج: ملخص للمبتدئ + آلية دقيقة للمحترف |

**قسم جديد**: `/no/fagomrader/innsikt/[slug]/` و`/en/use-cases/insights/[slug]/`
— مركن ضمن مساحة fagomrader الحالية (رابط من صفحة الفهرس)، لا قسمًا معزولًا.
كل مقال اتجاه يربط بصفحة المهنة الأقرب إن وُجدت (`relatedUseCases`، الحقل
موجود أصلًا في المخطط).

---

## 3. خط الأنابيب: اكتشاف → تسجيل نقاط → كتابة → نشر

### 3.1 مصادر الاكتشاف (allowlist صريحة، بلا مفاتيح API جديدة)

ملف جديد `content/topic-sources.yaml` (نفس روح `content/sources.yaml` لـ
Skills — بيانات لا أوامر، تُقرأ فقط من هذه القائمة):

| المصدر | النوع | ماذا يعطينا | يحتاج مفتاح؟ |
|---|---|---|---|
| `docs.anthropic.com` changelog/blog | رسمي | ميزات وقدرات جديدة فعليًا | لا |
| `code.claude.com/docs/en/claude_code_docs_map.md` | رسمي | نفس المصدر المستخدم في `watch-upstream.ts` | لا |
| Hacker News Algolia API (`hn.algolia.com/api/v1/search?query=Claude...`) | عام، بلا مفتاح | نقاط + تعليقات = إشارة اهتمام حقيقية | لا |
| GitHub Search API (`api.github.com/search/repositories?q=claude-code...`) | عام، معدّل طلبات منخفض بلا توثيق | نمو المستودعات ذات الصلة = إشارة اتجاه تقني | لا (يُفضَّل `GITHUB_TOKEN` الموجود أصلًا لرفع الحد) |
| `anthropic.com/news` | رسمي (مستخدم أصلًا) | إعلانات المنتج | لا |

**عمدًا مستبعد**: Reddit/Twitter/Google Trends — تحتاج مصادقة OAuth أو مفاتيح
مدفوعة، وتخالف مبدأ "بلا أسرار جديدة" الذي التزمنا به في كل الأتمتة السابقة.
إن رغبت لاحقًا بإضافتها، تتطلب قرارًا صريحًا منفصلًا (سرّ جديد + تقييم شروط
الاستخدام) — ليست جزءًا من هذه المرحلة.

### 3.2 `scripts/topics/discover.ts` — يوم الثلاثاء 05:00 UTC (بعد use-cases الاثنين بيوم، لا تعارض)

1. يجلب المصادر الأربعة أعلاه (نفس نمط `fetchText` بإعادة محاولة الموجود في
   `research.ts`).
2. **Haiku يستخلص** قائمة "مواضيع مرشحة" من النصوص الخام بأداة منظَّمة
   (tool_use)، كل مرشح: `{ topic, why_it_matters, source_excerpt_ref }`.
   **القاعدة الحرجة**: النموذج يستخلص ويقترح فقط — لا يسجّل النقاط. التسجيل
   حتمي في الكود (§3.3) لمنع "تلاعب" النموذج بالترتيب — نفس مبدأ فرض
   `maturity: experimental` برمجيًا في `skills-discover.ts`.
3. يقارن كل مرشح بـ **دفتر المواضيع** `content/snapshots/topics-ledger.json`
   (قائمة كل عنوان نُشر أو رُفض سابقًا + تاريخه) لمنع التكرار — يوازي دور
   `content/snapshots/skills/*.hash`.

### 3.3 تسجيل النقاط — حتمي، مفسَّر، غير قابل للتلاعب من النموذج

دالة TypeScript صرفة (لا LLM) في `scripts/topics/score.ts`:

```
score = (frequency × 0.35) + (recency × 0.25) + (engagement × 0.25) + (gap × 0.15)
```

- **frequency**: كم مصدرًا مستقلًا من الأربعة ذكر هذا الموضوع هذا الأسبوع (0-4).
- **recency**: وزن أعلى للمحتوى المنشور خلال آخر 14 يومًا (انحدار خطي بعدها).
- **engagement**: نقاط+تعليقات HN مُطبَّعة (0-1) — إشارة اهتمام بشرية حقيقية،
  ليست تخمين نموذج.
- **gap**: 1 إن لم يوجد مقال use-case أو skill يغطي الموضوع فعلًا (يُفحص عبر
  دفتر المواضيع + عناوين `content/use-cases/*` و`content/skills/*` الحالية)،
  0 إن كان مغطى حديثًا — **يمنع تكرار محتوى موجود بدل الاحتفاء بالتكرار
  كـ"شعبية"**.

**عتبة نشر**: `score ≥ 0.6` **و** موضوعان مستقلان على الأقل ذكراه (يمنع مقالًا
عن ذكر عابر واحد). أعلى **مرشحين اثنين فقط لكل تشغيل** يتقدمان للكتابة —
نفس سقف "جودة > كمية" المتّبع في `skills-discover` (حده الأقصى 2).

### 3.4 `scripts/topics/write.ts` — الكتابة المزدوجة العمق

البرومبت (Sonnet — الكتابة التفسيرية تستحق النموذج الأقوى، كما في
`skills/discover.ts`) يفرض قالبًا إلزاميًا بست فقرات، مصمَّم خصيصًا لإجابة
طلبك: **"الكل يفهم، المبتدئ والمحترف"**:

1. **الخلاصة السريعة** (2-3 أسطر) — للمستخدم العجول: ما الموضوع، لماذا الآن.
2. **ما الجديد بالضبط** — السياق: أي تغيير/إصدار/اتجاه أثار هذا.
3. **كيف يعمل تقنيًا** — الآلية الدقيقة خطوة بخطوة، لا تسويق. هذا القسم
   موجَّه للمحترف تحديدًا (يوازي "What it does" في قالب Skills لكن أعمق تقنيًا).
4. **تجربة عملية** — سيناريو TerminalSim أو مثال prompt→نتيجة حقيقي (يُعاد
   استخدام مكوّن `TerminalSim.svelte` الموجود، لا حاجة لبناء جديد).
5. **متى تستخدمه ومتى لا** — حدود صادقة (نفس المبدأ من قالب Skills).
6. **المصادر** — روابط شفافة لكل مصدر استُخدم (`sources: string[]` — الحقل
   موجود أصلًا في مخطط `useCases`).

المخرج JSON منظَّم (`record_topic_article` tool) يطابق حقول `useCases` schema
الحالية + حقلين جديدين: `trendScore` (رقم، يُكتب من الكود لا من النموذج) و
`discoveredAt` (تاريخ).

### 3.5 النشر

نفس `verifyAndPublish` الموجود حرفيًا — لا مسار نشر جديد. `maturity` هنا
يقابله حقل `draft`: كل مقال مكتشف يُنشر بـ `draft: false` فقط إن اجتاز
`audit:topics` الجديد (قسم §4)، وإلا يبقى `draft: true` (المخطط الحالي يدعم
هذا الحقل أصلًا) وتُفتح Issue للمراجعة البشرية — تمامًا كيف `experimental`
تحمي Skills من هلوسة الجودة.

---

## 4. البوابات (تمتد الأنماط الموجودة، لا تُخترع من الصفر)

- **مخطط Zod جديد `topics`** (أو حقلان إضافيان على `useCases` — القرار
  التنفيذي: أوصي بمخطط منفصل `content/topics/` لتفادي خلط دورة حياة "ثابت"
  و"مكتشَف" في نفس المجلد، تمامًا كما فصلنا `skills` عن `modules`).
- **`scripts/audits/audit-topics.ts`**: يتحقق من الأقسام الستة إلزامية بلغتيها
  (نفس منطق `REQUIRED_HEADINGS` في `audit-skills.ts`)، و`sources.length ≥ 1`،
  و`trendScore` رقم منطقي (0-1)، وعدم تكرار slug مع دفتر المواضيع.
- **`post-deploy-smoke`**: يضاف فحص لـ `/no/fagomrader/innsikt/` و
  `/en/use-cases/insights/` بعد أول نشر فعلي (لا قبل — تجنّب فحص صفحة فارغة).
- **سقف tokens**: نفس متغيرات `MAX_INPUT_TOKENS`/`MAX_OUTPUT_TOKENS` في
  `_auto-publish.ts`، مضبوطة في workflow الجديد.

---

## 5. التقويم المحدَّث (لا تعارض مع الموجود)

| التوقيت (UTC) | Workflow | التردد |
|---|---|---|
| الاثنين 05:00 | Use-case content update (موجود) | أسبوعي |
| **الثلاثاء 05:00** | **Topics discover (جديد)** | أسبوعي |
| الأربعاء 05:00 | Skills discover (موجود) | أسبوعي |
| الخميس 06:00 | **Topics write + publish (جديد)** — يوم منفصل عن discover عمدًا لإتاحة نافذة مراجعة بشرية اختيارية بينهما قبل الكتابة الفعلية بـ Sonnet | أسبوعي |

فصل discover عن write ليوم كامل قرار مقصود: التسجيل رخيص (Haiku)، الكتابة
مكلفة (Sonnet) — الفصل يتيح لي مراجعة قائمة المرشحين المُسجَّلين نقطيًا قبل
إنفاق استدعاء Sonnet عليها، عبر Issue تلقائية تُفتح بعد discover تسرد
المرشحين ونقاطهم (بدون كتابة فعلية بعد) — بوابة مراجعة خفيفة لا تعطّل
الاستقلالية الكاملة لاحقًا إن ثبتت الثقة بالجودة بعد بضع دورات.

---

## 6. خارطة KiMI — 3 PRs ببوابات مراجعة

**PR-A: البنية والاكتشاف**
- `content/topic-sources.yaml` + مخطط Zod.
- `scripts/topics/discover.ts` + `scripts/topics/score.ts` (اختبار وحدة
  بسيط لدالة `score()` بمدخلات معروفة — دالة حتمية يسهل اختبارها).
- workflow `topics-discover.yml` الثلاثاء، ينشر **تقرير المرشحين فقط** إلى
  Issue (بلا كتابة مقالات بعد).
- 🔎 **بوابة A**: أراجع جودة الاستخلاص ومنطقية درجات التسجيل على تشغيل حقيقي
  واحد قبل بناء مرحلة الكتابة فوقها.

**PR-B: الكتابة والنشر**
- `content/topics/` collection + `scripts/topics/write.ts` + `audit-topics.ts`.
- workflow `topics-write.yml` الخميس، بسقف مقالين/أسبوع.
- صفحات `/[lang]/fagomrader/innsikt/[slug]/` (أو `/use-cases/insights/`)
  بنمط صفحة `[slug].astro` لـ use-cases الحالية.
- 🔎 **بوابة B (الأهم)**: نفس بروتوكول `SKIP_PUBLISH=1` — dry-run لمقال واحد
  كامل، أراجعه بلغتيه قبل أول تشغيل مجدول حقيقي. هذا الشرط غير قابل للتفاوض،
  كما في skills-discover.

**PR-C: الدمج والحراسة**
- رابط "اكتشافات حديثة" في صفحة فهرس fagomrader/use-cases.
- توسيع `post-deploy-smoke` وربط دفتر المواضيع بـ `link-rot`/`freshness-heal`
  إن استُخدمت روابط مصادر مشتركة.
- 🔎 **بوابة C**: تحقق شامل + أسبوع مراقبة قبل اعتباره جزءًا من "hands-off".

---

## 7. لماذا هذا يجيب طلبك تحديدًا

- **"بحث عن مواضيع جديدة متعلقة بكلود/AI"** → §3.1–3.2: مصادر رسمية وHN
  وGitHub، لا قائمة يدوية.
- **"اختيار حسب قوة الترند أو الاحتياج بطريقة ذكية"** → §3.3: صيغة تسجيل
  نقاط حتمية مفسَّرة (لا "شعور" من نموذج)، بأربعة عوامل واضحة يمكن تعديل
  أوزانها لاحقًا حسب الأداء الفعلي.
- **"الكتابة ذكية لكن تشرح بدقة، يفهمها المبتدئ والمحترف"** → §3.4: قالب
  ست فقرات إلزامي يفصل "الخلاصة السريعة" (مبتدئ) عن "كيف يعمل تقنيًا"
  (محترف) صراحة، بدل خلطهما في نص واحد غامض.
