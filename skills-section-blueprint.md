# Skills Section Blueprint — خطة تنفيذية لوكيل KiMI

> **الأدوار:** KiMI هو المنفِّذ. Claude (Fable) هو الموجِّه والمراجع.
> KiMI لا يتجاوز بوابة مراجعة (Review Gate) قبل اعتمادها. كل مرحلة تنتهي بـ PR
> منفصل يراجعه Claude قبل الدمج.
>
> **القاعدة الذهبية:** هذا الموقع منصّة **تعلّم**، وليس دليل تثبيت. قسم Skills
> الجديد يجب أن يعلّم قبل أن يفهرس. أي قرار تصميمي يُقاس بسؤال واحد:
> "هل يجعل المستخدم يفهم المهارة، أم فقط ينسخ أمر تثبيتها؟"

---

## 1. تحليل الوضع الحالي — claude.mutaz.no

### 1.1 البنية التقنية
- **Astro 5** ثابت البناء (static)، جزر **Svelte** تفاعلية فقط عند الحاجة
  (TerminalSim, Quiz, ConfigBuilder, SearchPalette, ProgressMap, ThemeToggle).
- **مجموعات محتوى** مع تحقق **Zod** صارم في `src/content.config.ts` — البناء
  يفشل عند أي محتوى مخالف. هذا أهم أصل في المشروع ويجب أن يمتد للقسم الجديد.
- **ثنائية اللغة EN/NO** عبر `src/pages/[lang]/` مع تدقيق تكافؤ
  (`audit:parity`) يضمن ألا تتخلف لغة عن الأخرى.
- **أتمتة upstream**: `watch-upstream.ts` + `generate-update.ts` يرصدان إصدارات
  Claude Code وينشئان PR تلقائيًا — نموذج جاهز لإعادة الاستخدام مع Skills.
- **جودة مفروضة في CI**: Lighthouse ≥ 90/95، axe-core صفر انتهاكات،
  ميزانية JS < 60KB للصفحة، تدقيقات روابط/quiz/terminal/parity.
- **SEO منظّم**: JSON-LD لكل صفحة، OG images مولّدة، sitemap، hreflang.

### 1.2 بنية المعلومات الحالية
```
/[lang]/                    الرئيسية
/[lang]/learn/              10 وحدات تعليمية متسلسلة
/[lang]/learn/[module]      وحدة: MDX + TerminalSim + Quiz
/[lang]/playground          طرفية حرة
/[lang]/build               ConfigBuilder
/[lang]/reference           مرجع الأوامر
/[lang]/catalog             فهرس الموارد (modules فقط حاليًا)
/[lang]/quiz                اختبار مستوى
/[lang]/blog, /changelog, /feedback
/no/guider, /no/fagomrader  محتوى نرويجي خاص
/en/guides, /en/use-cases   محتوى إنجليزي خاص
```

### 1.3 رحلة المستخدم الحالية
اختبار مستوى ← مسار وحدات متدرج ← تطبيق في playground ← مرجع سريع.
رحلة تعليمية خطية قوية، لكن لا يوجد "ماذا بعد التعلم؟" — قسم Skills هو
الإجابة الطبيعية: من "تعلمت Claude Code" إلى "أوسّع Claude Code".

### 1.4 نقاط القوة والضعف

| قوة | ضعف / فجوة |
|---|---|
| تعلّم تفاعلي حقيقي (TerminalSim) نادر في السوق | لا محتوى عن Skills إطلاقًا رغم أنها أهم ميزة حديثة |
| جودة مفروضة آليًا (Zod, audits, CI) | `/catalog` هزيل — قائمة روابط لا قيمة مضافة فيها |
| ثنائية لغة مع تكافؤ مفروض | لا محتوى مجتمعي أو قابل للتحديث المتكرر (ما عدا changelog) |
| أتمتة رصد upstream ناضجة | وحدة `07-hooks` تجاور Skills لكن لا وحدة `skills` تعليمية |
| هوية واضحة: "تعلّم بالكتابة لا بالتصفح" | لا مسار من "أتعلم" إلى "أبني وأوسّع" |

---

## 2. تحليل skills.sh

### 2.1 كيف يعمل
- **Leaderboard** مركزي: ترتيب بعدد التثبيتات + نشاط 8 أسابيع.
- تصفية حسب: الكل / Trending 24h / Hot / Official / Security-audited /
  مواضيع (React, Design, Databases, Marketing…) / منصة الوكيل
  (Claude Code, Cursor, Copilot, +13).
- **بيانات كل مهارة**: الاسم، المستودع `owner/repo`، عدد التثبيتات، النشاط.
- **تثبيت بأمر واحد**: `npx skills add <owner/repo>`.
- بحث نصي بسيط، صفحات تفصيل مختصرة، شارات Official/Audited.

### 2.2 قوة وضعف skills.sh

| قوة | ضعف (فرصتنا) |
|---|---|
| اكتشاف فوري + تثبيت بأمر واحد | **صفر محتوى تعليمي** — لا يشرح ما الـ skill أصلًا ولا كيف تعمل آلية التفعيل |
| إشارة شعبية (installs) تبني الثقة | الشعبية ≠ الجودة؛ لا تقييم تحريري ولا معايير |
| تغطية منصات متعددة | لا شفافية أمان فعلية: ماذا تنفّذ المهارة؟ أي سكربتات تحملها؟ أي صلاحيات تحتاج؟ |
| Trending يعطي حيوية | لا سياق استخدام: متى تختار هذه لا تلك؟ لا مقارنات، لا حدود (limitations) |
| بساطة UX | أحادي اللغة، لا مسار تعلم، لا ربط بمفاهيم (hooks/subagents/MCP) |

### 2.3 الخلاصة الاستراتيجية
skills.sh = **دليل هاتف**. نحن نبني **دليل ميداني (Field Guide)**:
أقل عددًا، أعمق شرحًا، مقيَّم تحريريًا، ثنائي اللغة، مربوط بمسار التعلم.
**لا نتنافس على العدد** (804K+ تثبيت لن نجاريه) — نتنافس على الفهم والثقة.

### 2.4 افتراضات نتحداها عمدًا
1. **"الأكثر تثبيتًا = الأفضل"** ← نستبدلها بتقييم تحريري ببطاقة معايير
   (rubric) معلنة + ذكر عدد التثبيتات كسياق لا كترتيب.
2. **"الدليل يجب أن يكون شاملًا"** ← كتالوج منسّق (curated) من 20–30 مهارة
   ممتازة يتفوق على 800 صفحة فارغة، خاصة لـ SEO (لا thin content).
3. **"صفحة المهارة = بطاقة تثبيت"** ← صفحتنا درس مصغّر: تشريح المهارة،
   تجربة حية، متى لا تستخدمها.
4. **"Skills قسم مستقل"** ← بل عضو في منظومة: وحدة تعليمية جديدة
   `11-skills` تشرح المفهوم، والقسم يطبّقه، وConfigBuilder يولّد سكربت تثبيت.

---

## 3. التموضع وبنية المعلومات للقسم الجديد

### 3.1 التموضع في التنقل
يضاف **Skills** في التنقل الرئيسي بعد **Learn** مباشرة (المنطق: تعلّم ← وسّع).
لا نلمس بقية البنية. صفحة `/catalog` تُبقى وتضاف إليها المهارات كـ `kind: skill`.

### 3.2 خريطة URLs
```
/[lang]/skills/                      الصفحة الأم: نظرة عامة + مصفوفة التصنيفات
/[lang]/skills/[slug]/               صفحة مهارة (flat — بدون تصنيف في المسار)
/[lang]/skills/category/[category]/  صفحة تصنيف
/[lang]/skills/compare/              مقارنات منسّقة (مرحلة 2)
/[lang]/learn/skills/                وحدة تعليمية جديدة: "What are Skills?"
```
**قرار مقصود:** slug مسطّح بدون التصنيف في المسار — لأن المهارة قد تنتمي
لتصنيفين، ولأن تغيير التصنيف لاحقًا لا يكسر الروابط. التصنيف وسم (taxonomy)
لا تسلسل هرمي.

### 3.3 التصنيفات (6 فقط — لا نستنسخ تصنيفات skills.sh)
| slug | EN | NO |
|---|---|---|
| `documents` | Documents & Files | Dokumenter og filer |
| `code-quality` | Code Quality & Review | Kodekvalitet |
| `web-frontend` | Web & Frontend | Web og frontend |
| `automation` | Automation & Agents | Automatisering og agenter |
| `data-research` | Data & Research | Data og research |
| `writing-marketing` | Writing & Marketing | Skriving og markedsføring |

قاعدة: تصنيف جديد فقط عندما يمتلك ≥ 3 مهارات منشورة.

### 3.4 نموذج المحتوى (يتبع نمط الوحدات الحالي)
```
content/skills/<slug>/
  meta.yaml    # مخطط Zod جديد `skills` في content.config.ts
  en.mdx
  no.mdx
```
حقول `meta.yaml` الإلزامية:
`slug`, `title{en,no}`, `summary{en,no}`, `categories[]` (1–2),
`source{repo, path, license}`, `install{command}`, `maturity`
(official | community | experimental), `level` (beginner | intermediate |
advanced), `worksWithout` (هل تعمل بدون أدوات خارجية؟),
`permissions[]` (ماذا تنفّذ: bash, file-write, network…),
`relatedSkills[]`, `relatedModules[]`, `lastVerified` (تاريخ),
`verifiedVersion` (إصدار Claude Code الذي جُرّبت عليه),
`rubric{docs, safety, reliability, focus}` (درجات 1–5 بمعايير معلنة).

### 3.5 الربط الداخلي (إلزامي، يفحصه audit)
- كل صفحة مهارة ← وحدة/وحدات تعلم ذات صلة + 2–4 مهارات ذات صلة + تصنيفها.
- كل وحدة تعلم ذات علاقة (hooks, slash-commands, mcp-servers, subagents) ←
  فقرة "Related skills" تشير للقسم الجديد.
- الصفحة الأم `/skills/` ← وحدة `learn/skills` بعبارة "لا تعرف ما الـ Skill؟
  ابدأ هنا" (خدمة المبتدئ).
- `use-cases` و`guides` تربط بالمهارات التي تخدم سيناريوهاتها.

---

## 4. تشريح صفحة المهارة (القالب الملزم لكل صفحة)

بالترتيب — والأقسام 1–7 إلزامية، 8–10 حسب الحالة:

1. **رأس الصفحة**: الاسم، الملخص (سطران)، شارات (maturity/level/category)،
   بطاقة `lastVerified` + `verifiedVersion` ظاهرة — هذه عملة الثقة.
2. **ماذا تفعل ولماذا توجد** (≤ 150 كلمة): المشكلة قبل الحل.
3. **جرّبها الآن — TerminalSim**: سيناريو موجّه يحاكي التثبيت والتفعيل
   وناتجًا حقيقيًا. هذا ما لا يملكه skills.sh ولن يقلده بسهولة.
4. **التثبيت**: الأمر + أين تُوضع الملفات (`~/.claude/skills/` مقابل
   `.claude/skills/`) + كيف تتحقق أنها فُعّلت.
5. **بطاقة الأمان والصلاحيات**: ماذا تنفّذ المهارة (سكربتات؟ شبكة؟ كتابة
   ملفات؟)، من مصدرها، رخصتها، وتنبيه صريح إن كانت تشغّل كودًا.
   **ميزة تنافسية أساسية — لا أحد يقدمها بجدية.**
6. **حالات استخدام عملية** (2–3): prompt حقيقي ← ناتج حقيقي (مختصر).
7. **الحدود ومتى لا تستخدمها**: قسم إلزامي. المصداقية تُبنى هنا.
8. **تشريح SKILL.md** (للمهارات التعليمية القيمة): كيف كُتب الـ frontmatter
   والـ description ولماذا يتفعّل — يحول الصفحة لدرس في بناء المهارات.
9. **بدائل ومقارنة سريعة**: جدول صغير مقابل 1–2 بديل.
10. **مراجع**: المستودع، التوثيق الرسمي، وحدات التعلم ذات الصلة.

---

## 5. ميزات تتجاوز "الدليل البسيط"

| الميزة | القيمة | المرحلة |
|---|---|---|
| TerminalSim لكل مهارة | تجربة قبل التثبيت — التفوق الجوهري | MVP |
| بطاقة أمان/صلاحيات | ثقة لا يوفرها أي دليل | MVP |
| Rubric تحريري معلن (4 معايير × 5) | بديل عن سباق التثبيتات | MVP |
| `lastVerified` + تدقيق آلي للطزاجة | يحارب تعفن المحتوى | MVP (الحقل) / P2 (الأتمتة) |
| وحدة تعلم `learn/skills` + امتداد الـ quiz | يخدم المبتدئ قبل الدليل | MVP |
| صفحات مقارنة منسّقة (`/skills/compare/`) | يجيب "أيهما أختار؟" | P2 |
| ConfigBuilder: توليد أوامر تثبيت لحزمة مختارة | جسر مع أداة موجودة | P2 |
| "Starter packs" — حزم بحسب الدور (كاتب/مطور/مسوّق) | منسّق > خوارزمي | P2 |
| مسار "ابنِ مهارتك الأولى" التفاعلي (skill-creator) | من مستهلك إلى منتج | مستقبل |
| تقييمات مستخدمين | مؤجلة عمدًا — تحتاج backend والموقع ثابت؛ الإشارة التحريرية تكفي الآن | مستقبل |

---

## 6. الصيانة والتحديث

1. **سكربت `scripts/skills/verify.ts`** (P2): لكل مهارة يجلب مستودعها المصدر،
   يقارن hash ملف SKILL.md مع snapshot محفوظ في `content/snapshots/skills/`،
   وعند التغيير ينشئ Issue/PR — نفس نمط `watch-upstream.ts` الموجود.
2. **`audit:skills`** يضاف لسلسلة `npm run audit`: تحقق parity EN/NO،
   صحة `relatedSkills/relatedModules` (لا روابط ميتة)، وأن `lastVerified`
   أحدث من 90 يومًا (تحذير) / 180 يومًا (فشل).
3. **سياسة تحرير**: مهارة جديدة = PR بقالب جاهز؛ لا تُنشر تحت
   `maturity: official/community` إلا بعد تجربة فعلية موثقة في وصف الـ PR.
4. **دورة ربع سنوية**: مراجعة التصنيفات، ترقية/إنزال maturity، أرشفة
   المهارات المهجورة (تبقى الصفحة مع لافتة "archived" — لا نكسر الروابط).

---

## 7. تجربة المستخدم حسب المستوى

- **مبتدئ**: لا يُرمى في الكتالوج. `/skills/` تفتح بشريط "ما هي المهارات؟"
  ← وحدة `learn/skills` ← ثم "5 مهارات آمنة تبدأ بها" (worksWithout: true،
  بلا صلاحيات خطرة). فلتر level=beginner افتراضي عند القدوم من مسار التعلم.
- **متوسط**: الكتالوج بفلاتر (تصنيف/level/maturity/الصلاحيات) + صفحات
  المقارنة + starter packs حسب الدور.
- **متقدم**: بطاقة الصلاحيات التفصيلية، تشريح SKILL.md، روابط المصدر مباشرة،
  ثم مسار "ابنِ مهارتك". الوصول السريع عبر SearchPalette (⌘K) بعد فهرسة
  المهارات فيها.

---

## 8. خارطة الطريق — مهام KiMI وبوابات مراجعة Claude

> كل مرحلة = فرع + PR مستقل. KiMI يشغّل قبل كل PR:
> `npm run check && npm run audit && npm run build` ويرفق النواتج.
> **Claude يراجع ويقرر الدمج. KiMI لا يدمج بنفسه.**

### المرحلة 0 — MVP (الأساس) — تقدير: 3 PRs

**PR-1: البنية التحتية للمحتوى**
- مجموعة `skills` في `src/content.config.ts` بمخطط Zod كامل (حقول §3.4).
- مجلد `content/skills/` + مهارة نموذجية واحدة كاملة (مقترح: `pdf` أو
  `frontend-design`) بكل أقسام القالب §4 بلغتين.
- صفحات `/[lang]/skills/index.astro` و`/[lang]/skills/[slug].astro`
  بنمط صفحات `learn` الحالية (نفس المكونات، نفس نظام التصميم، JSON-LD
  مناسب — `HowTo` أو `SoftwareApplication` + `BreadcrumbList`).
- 🔎 **بوابة مراجعة 1**: يراجع Claude المخطط والقالب قبل كتابة بقية المحتوى
  — تغيير المخطط بعد 20 مهارة مكلف.

**PR-2: المحتوى الأولي + التصنيفات**
- 10–12 مهارة منشورة تغطي التصنيفات الستة (≥ 1 لكل تصنيف؛ الأولوية للمهارات
  الرسمية: pdf, docx, pptx, xlsx + الأكثر قيمة مجتمعيًا: frontend-design,
  find-skills, seo-audit…).
- صفحات التصنيف `/[lang]/skills/category/[category]/`.
- سيناريو TerminalSim لكل مهارة (يتبع نمط `terminal.yaml` الحالي).
- 🔎 **بوابة مراجعة 2**: تدقيق تحريري — دقة تقنية، جودة النرويجية، اكتمال
  بطاقات الأمان، درجات rubric مبررة.

**PR-3: الدمج في المنظومة**
- إضافة Skills إلى Nav وFooter وSearchPalette وصفحة `/catalog`
  (`kind: skill`) والصفحة الرئيسية (بطاقة تعريف بالقسم).
- وحدة تعليمية `11-skills` في `content/modules/` (meta/terminal/quiz/en/no)
  تشرح: ما الـ skill، بنية SKILL.md، الفرق عن slash-commands وMCP وsubagents،
  اعتبارات الأمان.
- روابط "Related skills" في الوحدات 04/05/06/07.
- `audit:skills` مضاف إلى `npm run audit` وإلى CI.
- 🔎 **بوابة مراجعة 3**: مراجعة شاملة + Lighthouse/axe + تجربة رحلة كاملة
  (مبتدئ من الرئيسية حتى تثبيت مهارة) قبل النشر.

### المرحلة 2 — التعميق — تقدير: 2–3 PRs
- التوسع إلى 25–30 مهارة.
- صفحات مقارنة منسّقة (2–3 مقارنات عالية الطلب).
- Starter packs حسب الدور + توليد أوامر التثبيت من ConfigBuilder.
- أتمتة `scripts/skills/verify.ts` + snapshots + workflow دوري.
- فلاتر الكتالوج (تصنيف/level/maturity) — جزيرة Svelte خفيفة ضمن ميزانية
  الـ 60KB.
- 🔎 بوابة مراجعة لكل PR كالمعتاد.

### المستقبل
- مسار تفاعلي "ابنِ مهارتك الأولى" (تكامل مفاهيمي مع skill-creator).
- شارة "verified on vX.Y" مرتبطة بأتمتة upstream الحالية.
- تقييمات/تعليقات مستخدمين (فقط إذا أُضيف backend — قرار منفصل).
- RSS/JSON feed للمهارات المضافة حديثًا.

---

## 9. معايير القبول العامة (تنطبق على كل PR)

1. `npm run check` و`npm run audit` و`npm run build` تنجح.
2. تكافؤ EN/NO كامل — لا صفحة بلغة واحدة.
3. Lighthouse: perf ≥ 90، a11y ≥ 95، SEO ≥ 95؛ JS < 60KB للصفحة.
4. axe-core: صفر انتهاكات serious/critical.
5. كل مهارة منشورة تحتوي الأقسام الإلزامية 1–7 من §4 بلا استثناء.
6. لا روابط داخلية ميتة (`audit:links`).
7. JSON-LD صالح لكل صفحة جديدة + OG image مولّدة.
8. لا تغيير في صفحات/مسارات موجودة خارج نطاق المرحلة دون موافقة المراجع.
