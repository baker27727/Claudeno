---
title: "What's new in Claude Code v2.1.206"
description: "Added directory path suggestions to `/cd` and expanded `/doctor` into a full setup checkup; `/commit-push-pr` now auto-allows git push to the configured…"
pubDate: "2026-07-10T09:32:59.935Z"
author: "Claude Code Learn"
tags:
  - "changelog"
  - "release"
sources:
      - "https://code.claude.com/docs/en/claude_code_docs_map.md"
draft: false
---

Added directory path suggestions to `/cd` and expanded `/doctor` into a full setup checkup; `/commit-push-pr` now auto-allows git push to the configured push remote; `/login` supports Anthropic-operated gateway endpoints. Background agents now upgrade immediately after Claude Code updates. MCP servers properly respect per-server request timeout settings. Fixed multiple critical issues in subagents, authentication, and MCP server connectivity.

**Sources:**
- https://code.claude.com/docs/en/claude_code_docs_map.md
