---
title: "What's new in Claude Code v2.1.268"
description: "Gateway pricing support with signed-in rate matching; startup warnings for empty access control; new `gatewayInternalNetworks` managed setting; `claude…"
pubDate: "2026-09-11T10:38:41.317Z"
author: "Claude Code Learn"
tags:
  - "changelog"
  - "release"
sources:
      - "https://code.claude.com/docs/en/claude_code_docs_map.md"
draft: false
---

Gateway pricing support with signed-in rate matching; startup warnings for empty access control; new `gatewayInternalNetworks` managed setting; `claude self-hosted-runner --remove-session-state` flag; `claude auth status --json configDirectory`; `--json` output for plugin commands with error/note details; WebFetch 300-second timeout with `CLAUDE_CODE_WEBFETCH_DEADLINE_MS` override; artifact tool regex fix for third-party Anthropic-compatible endpoints; respawned teammates no longer pick up stale agent files; CPU-usage fixes for idle sessions and terminal focus reports; WebFetch error fix for localhost; MCP and plugin error messages scrubbed of secrets and credentials; prompt-cache and extended-thinking stability for SDK sessions with `excludeDynamicSections`; model-access denial cache freshness improvements; Fable long-context 429 messaging fix; workload identity federation jti-reuse fix; OAuth redirect port availability error messaging; `/compact` text-mangling fix for `$` sequences; conversation-recall suggestions fix; agent list navigation responsiveness; session-delete error messaging for worktrees; Slack tool loss in sessions with MCP allowlist fix; Chrome navigation URL scheme parsing fix; terminal spinner and cursor display improvements; remote control session naming in `ListAgents`; plugin path validation for dot-prefixed directories; WebFetch localhost error messaging; PermissionRequest hooks in print mode; policy-helper warnings in headless runs; `/resume` fork-session naming; cloud-session commands suggest `/login` when signed out; SessionEnd hook timeout extension; `/autofix-pr` GitHub account connection error messaging; cloud-session policy explanation; Bash sandbox instruction accuracy; fullscreen mode repainting performance; `--continue`/`--resume` immediate display and single transcript read; tool-heavy turn responsiveness; project workflow startup time; auto mode denial messaging improvements; Claude in Chrome page-read inlining; MEMORY.md truncation warning detail; artifact terminal permission prompt leading question; prompt footer editor/diff selection display; 1M-context usage-credits message clarification; `/plugin` menu effect immediacy; Bedrock/Vertex/Foundry system-prompt attachment parity; tool-list byte stability on late-connecting tools; task-tracking tool model restrictions; artifact data-edit permission card; local Cowork file-access security; WebFetch deny/ask-rule scope clarification with Artifact tool; MCP server auth startup notice frequency; VSCode session-list and settings-toggle fixes with `CLAUDE_CONFIG_DIR`; VSCode model-pill responsiveness across login/logout; VSCode Auto-mode persistence in open tabs; VSCode SessionStart hook session-name stability; VSCode footer-pill startup synchronization; VSCode rapid session-tab launch handling; VSCode `preferredLocation` sidebar resumption; VSCode Windows WSL and diagnostics fixes; VSCode custom-style-builder folder; VSCode permission-rule arrow-key navigation and focus commands; VSCode Manage-plugins dialog session application; VSCode artifact permission-prompt `don't-ask-again` omission parity; cloud-session file-persistence for up to a day; cloud-session effort-level resume validation; routine-creation connector guidance; Claude Tag admin-settings Retry button; plugin marketplace `command` sources with `mode: link`; SSE keepalive-pings during long thinking; `ListAgents` offline and cloud labels.

**Sources:**
- https://code.claude.com/docs/en/claude_code_docs_map.md
