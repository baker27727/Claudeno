---
title: "Slash Commands"
description: "Manage context, switch models, undo mistakes, and configure your session without leaving the conversation."
---
import Callout from "../../../src/components/Callout.astro";

# Slash Commands

Inside an interactive session, commands starting with `/` control the session itself —
your context, your model, your history — rather than asking Claude to do something in
your code. A command is only recognized at the start of your message; anything after
it becomes an argument. When in doubt, this always works:

```bash
/help
```

## Managing your context

Two commands sound similar but do very different things.

```bash
/clear
```

`/clear` starts a brand new conversation with completely empty context. The old
conversation isn't gone — it's just parked; you can reopen it later. Aliases: `/reset`,
`/new`.

```bash
/compact
```

`/compact` frees up space by **summarizing** the conversation so far, instead of
wiping it. You keep working in the same conversation, just with a lighter context
window.

<Callout type="tip" title="Tip">
  Use `/clear` when you're switching to a genuinely unrelated task. Use `/compact`
  when you're still working on the same thing but the conversation has gotten long.
</Callout>

## Continuing later

```bash
/resume
```

`/resume` reopens a past conversation by picking it from a list, including ones you
left behind with `/clear`. Alias: `/continue`. This is different from `-c`/`--continue`
at the CLI level (covered in [CLI Basics](/en/learn/cli-basics)), which always resumes
the *most recent* conversation without a picker.

## Switching models mid-session

```bash
/model
```

`/model` opens a picker to switch which Claude model the session uses, and saves your
choice as the default for new sessions. No need to restart Claude Code to try a
different model.

## Undoing a mistake

```bash
/rewind
```

`/rewind` steps back to an earlier point — in the code, in the conversation, or both.
It's the closest thing to a safety net for when a change went somewhere you didn't
want. Aliases: `/checkpoint`, `/undo`.

<Callout type="mistake" title="Common mistake">
  Reaching for `/clear` to "undo" a bad edit just loses your context — it doesn't
  touch the files Claude changed. Use `/rewind` to actually roll back code.
</Callout>

## More commands worth knowing

| Command | Purpose |
|---|---|
| `/permissions` | Manage allow/ask/deny rules for tools. Alias `/allowed-tools`. |
| `/mcp` | Connect to, list, or reconnect MCP servers. |
| `/init` | Generate a starter `CLAUDE.md` for a new project. |

## Key takeaways

- `/clear` wipes context and starts fresh; `/compact` summarizes without wiping.
- `/resume` reopens any past conversation by name or ID — nothing is truly lost.
- `/model` switches models without restarting the session.
- `/rewind` is the real "undo" for code changes, not `/clear`.

## Related skills

A skill behaves like a slash command that can also trigger itself: `/name` works exactly like a custom command, but Claude also loads it automatically when your request matches its description. Learn how in [Skills & SKILL.md](/en/learn/skills/), or [browse the curated skills catalog](/en/skills/).

## What's new in v2.1.205

/doctor is now a full setup checkup that can diagnose and fix issues (in version 2.1.205). Use /checkup as an alias. Note that startup warnings like "claude command missing or broken" have moved from initial startup into /doctor and /status output.

## What's new in v2.1.206

The `/cd` command now provides directory path suggestions similar to the `/add-dir` behavior, making it easier to navigate to frequently used directories. Additionally, `/doctor` (aliased as `/checkup`) has been expanded into a full setup checkup tool that can diagnose and fix issues, including a new check that proposes trimming checked-in `CLAUDE.md` files by removing content Claude could derive from the codebase. The `/commit-push-pr` command now auto-allows `git push` to the repository's configured push remote (via `remote.pushDefault`, or the sole remote when only one is configured) in addition to `origin`. The `/login` command on the Gateway now supports Anthropic-operated public gateway endpoints.

## What's new in v2.1.207

The `/cd` command now provides directory path suggestions matching the behavior of `/add-dir`. The `/doctor` command has been expanded into a full setup checkup that can diagnose and fix issues (alias: `/checkup`), including a check that proposes trimming checked-in `CLAUDE.md` files by cutting content Claude could derive from the codebase.

## What's new in v2.1.212

**New `/fork` behavior**: `/fork` now copies your conversation into a new background session (visible as its own row in `claude agents`) while you continue working in the current session. The in-session subagent it used to launch is now available via `/subtask`. **New `/resume` picker**: Type `/resume` in the agent view to open a picker of past sessions—including deleted ones—and resume your choice as a background session.

## What's new in v2.1.214

The `/fork` command has been redesigned in version 2.1.212: it now copies your conversation into a new background session (a new row in `claude agents`) while you keep working in the current session. The previous in-session subagent launcher has been replaced by the `/subtask` command.

## What's new in v2.1.215

Claude no longer automatically runs `/verify` and `/code-review` skills. Invoke them explicitly with `/verify` or `/code-review` when you want them to execute.

## What's new in v2.1.217

The `/resume` and `/continue` commands now properly handle malformed attachment entries in transcripts instead of failing with a TypeError. The `/compact` command works correctly for Claude Opus 4.8 on Bedrock and no longer fails once over the limit.

## What's new in v2.1.218

The `/code-review` command now runs as a background subagent, keeping your conversation focused on other work while reviews are processed in the background. Similarly, `/deep-research` now starts only when invoked manually rather than launching automatically. The `/ultrareview` command has improved error handling to help Claude correct invalid arguments, and descriptive arguments like "review my auth changes" now apply as notes to findings on your current branch.

## What's new in v2.1.220

/code-review now runs as a background subagent, keeping review work out of your main conversation. `/deep-research` no longer launches automatically; it only starts when invoked manually. Dynamic workflows now default to a medium size guideline (fewer than 15 agents); adjust via the Dynamic workflow size option in `/config`.

## What's new in v2.1.221

New in 2.1.221: The `/plugin install` command now refreshes a stale marketplace catalog and retries before reporting a plugin not found. Additionally, plugins installed via `/plugin` now activate immediately when safe, instead of always requiring `/reload-plugins`. The `/status` command has been enhanced to show the session kind: `interactive`, or a background job that is `attached` or `unattached`.

## What's new in v2.1.223

The `/review` command is now an alias for `/code-review`, which reviews the current diff or a pull request (`/code-review <level> <pr#>`). Use `/code-review ultra` for a deep cloud review. When you type a level like `/code-review high`, it becomes your new default for subsequent reviews in that session.

## What's new in v2.1.224

New in 2.1.224: When removing unavailable paste changes that would alter a command's text, Claude Code now includes a cancel-and-confirm step to prevent accidental modifications.

## What's new in v2.1.226

**New in 2.1.226:** The `/teleport` command is available in cloud sessions, showing how to continue a session locally using `claude --teleport <session id>`.

## What's new in v2.1.227

**What's new in v2.1.227:** The slash-command menu has been improved—blue now marks only the selected row, matched characters are bolded instead of recolored, and emoji or accented names keep their glyphs. Also, `/code-review` (alias `/review`) now reuses your last effort level when called without a level, and you can specify depth like `/code-review ultra` for deep cloud reviews.

## What's new in v2.1.232

**Cross-session messaging (v2.1.232):** Type `@` in the prompt to mention another Claude session by name; Claude then uses `SendMessage` to reach that session directly. `SendMessage` now delivers to a bare name that exactly matches one live session, eliminating confirmation steps. Use `/config` to set "Messages from your other sessions" to accept, hold, or refuse inbound cross-session messages.

## What's new in v2.1.233

New in 2.1.233: The `/effort` selector in screen reader mode now renders as a numbered list with a typed-number prompt for improved accessibility.
