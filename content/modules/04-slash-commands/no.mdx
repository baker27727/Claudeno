---
title: "Slash-kommandoer"
description: "Håndter kontekst, bytt modell, angre feil, og konfigurer økten uten å forlate samtalen."
---
import Callout from "../../../src/components/Callout.astro";

# Slash-kommandoer

Inne i en interaktiv økt styrer kommandoer som starter med `/` selve økten — konteksten
din, modellen din, historikken din — i stedet for å be Claude gjøre noe i koden din. En
kommando gjenkjennes bare i starten av meldingen din; alt etter blir et argument. Når du er i tvil, funker alltid denne:

```bash
/help
```

Skriv `/` for å se alle tilgjengelige kommandoer, eller skriv `/` etterfulgt av bokstaver for å filtrere.

## Håndtere konteksten din

To kommandoer høres like ut, men gjør svært forskjellige ting.

```bash
/clear
```

`/clear` starter en helt ny samtale med fullstendig tom kontekst. Den gamle samtalen
er ikke borte — den er bare parkert; du kan åpne den igjen senere med `/resume`. Aliaser: `/reset`,
`/new`.

```bash
/compact
```

`/compact` frigjør plass ved å **oppsummere** samtalen så langt, i stedet for å tømme
den. Du fortsetter å jobbe i samme samtale, bare med et lettere kontekstvindu.

<Callout type="tip" title="Tips">
  Bruk `/clear` når du bytter til en helt urelatert oppgave. Bruk `/compact` når du
  fortsatt jobber med det samme, men samtalen har blitt lang.
</Callout>

## Fortsette senere

```bash
/resume
```

`/resume` åpner en tidligere samtale på nytt ved å velge den fra en liste, også de du
forlot med `/clear`. Alias: `/continue`. Dette er annerledes enn `-c`/`--continue` på
CLI-nivå (dekket i [CLI-grunnleggende](/no/learn/cli-basics)), som alltid gjenopptar
den *siste* samtalen uten en velger.

## Bytte modell midt i økten

```bash
/model
```

`/model` åpner en velger for å bytte hvilken Claude-modell økten bruker, og lagrer
valget som standard for nye økter. Ingen grunn til å restarte Claude Code for å prøve
en annen modell.

## Angre en feil

```bash
/rewind
```

`/rewind` går tilbake til et tidligere punkt — i koden, i samtalen, eller begge deler.
Det er det nærmeste du kommer et sikkerhetsnett når en endring gikk et sted du ikke
ville. Aliaser: `/checkpoint`, `/undo`.

<Callout type="mistake" title="Vanlig feil">
  Å bruke `/clear` for å "angre" en dårlig endring fjerner bare konteksten din — den
  rører ikke filene Claude endret. Bruk `/rewind` for faktisk å rulle tilbake kode.
</Callout>

## Flere kommandoer verdt å kjenne til

| Kommando | Formål |
|---|---|
| `/permissions` | Administrer allow/ask/deny-regler for verktøy. Alias `/allowed-tools`. |
| `/mcp` | Koble til, list opp, eller koble til på nytt MCP-tjenere. |
| `/init` | Generer en start-`CLAUDE.md` for et nytt prosjekt. |
| `/effort` | Juster innsatsnivå: `low` til `xhigh`, `max`, `ultracode`, eller `auto`. |
| `/advisor` | Aktiver eller deaktiver advisor-verktøyet for veiledning på kritiske momenter. |
| `/context` | Visualiser gjeldende kontekstbruk og optimiseringsforslag. |
| `/diff` | Åpne en interaktiv diff-visning som viser uforpliktende endringer. |
| `/code-review` | Gjennomgå gjeldende diff eller pull request for feil og oppryddinger. Alias: `/review`. |
| `/security-review` | Sjekk diffen for sikkerhetssårbarheter. |
| `/memory` | Forbedre prosjektets `CLAUDE.md`-minnefil. |
| `/batch` | Dekomponér en stor endring til uavhengige enheter og kjør hver parallelt. |
| `/fork` | Kopier gjeldende samtale til en ny bakgrunnsøkt. |
| `/background` | Løs gjeldende økt for å kjøre som bakgrunnsagent. Alias: `/bg`. |
| `/status` | Vis gjeldende øktstatus og kontekstbruk. |
| `/tasks` | List bakgrunnsarbeid og underagentoppgaver i gjeldende økt. |
| `/usage` | Vis tokenbruk og kostnader. Alias: `/cost`. |
| `/debug` | Aktiver debug-logging og feilsøk kjøretidsproblemer. |
| `/doctor` | Kjør en oppsettkontroll som diagnostiserer og fikser problemer. Alias: `/checkup`. |
| `/config` | Åpne Innstillinger for å justere tema, modell, utgangsstil og andre innstillinger. Alias: `/settings`. |
| `/theme` | Endre visuelt tema for økten. |

## Arbeidsflyter og skills

Noen kommandoer er bundlete arbeidsflyter eller skills som distribuerer arbeid på tvers av flere underagenter eller gir spesialisert veiledning:

| Kommando | Formål |
|---|---|
| `/batch` | **[Skill]** Orkestrér store endringer på tvers av kodekall parallelt. |
| `/code-review` | **[Skill]** Gjennomgå kode for korrekthetsfeil og oppryddingsmuligheter. |
| `/deep-research` | **[Workflow]** Distribuér webbsøk, hent og kryss-sjekk kilder, og syntetisér en sitert rapport. |
| `/design` | **[Skill]** Utkast UI-mockups, skjermflyter, målsider eller plakater som artboards. |
| `/dataviz` | **[Skill]** Designveiledning for diagrammer, grafer og dashboards. |
| `/verify` | **[Skill]** Verifiser at kodeendringer fungerer korrekt (påkall eksplisitt). |

## Oppsummering

- `/clear` tømmer konteksten og starter på nytt; `/compact` oppsummerer uten å tømme.
- `/resume` åpner enhver tidligere samtale på nytt via navn eller ID — ingenting er
  virkelig tapt.
- `/model` bytter modell uten å restarte økten.
- `/rewind` er den ekte "angre"-kommandoen for kodeendringer, ikke `/clear`.
- `/effort` og `/advisor` tilpasser hvordan Claude nærmer seg oppgaven din.
- `/code-review` kjører som en bakgrunnsunderagent, som holder konversasjonen din fokusert.

## Relaterte skills

En skill oppfører seg som en slash-kommando som også kan utløse seg selv: `/navn` fungerer akkurat som en egendefinert kommando, men Claude laster den også automatisk når forespørselen din matcher beskrivelsen. Lær hvordan i [Skills og SKILL.md](/no/learn/skills/), eller [utforsk den kuraterte skills-katalogen](/no/skills/).

## Versjonshistorikk

### v2.1.251
Kommandoen `/cost` viser nå en per-økt prompt-cache-linje med cache-treffratio, antall misser, gjenopphentede token og varm/kald cache-status.

### v2.1.250
Ny `/usage-credits`-kommando for Enterprise-organisasjoner. `/web-setup` advarer nå når GitHub CLI-tokenet mangler `workflow`-omfanget.

### v2.1.247
Ferdigheten `/claude-api` utvidet med Admin API-dekning og ny `/claude-api cost-optimize`-underkommando for profilring av utgifter.

### v2.1.246
Ny Automodus-flik i `/permissions` for å vise og redigere automodus klassifisererregler.

### v2.1.245
`/status` viser nå `Skipped sources`-linje og om GitHub er tilkoblet for Claude Code på nettet.

### v2.1.237
`/config` inkluderer nå en "Output style"-innstilling der du kan velge den nye "Concise"-stilen.

### v2.1.234
Du kan nå åpne `/permissions`, `/add-dir`, `/autocompact`, `/theme`, `/help`, `/config` og `/advisor`-dialoger mens Claude arbeider.

### v2.1.232
Meldinger på tvers av økter: Skriv `@` i ledeteksten for å nevne en annen Claude-økt ved navn.

### v2.1.227
Forbedringer i slash-kommandomenyen med bedre uthevelse og emoji-støtte. `/code-review` gjenbruker ditt siste innsatsnivå når det kalles uten nivå.

### v2.1.223
`/review` er nå et alias for `/code-review`, som kan kjøres som `/code-review ultra` for dype skygjennomganger.

### v2.1.220
`/code-review` kjører nå som en bakgrunnsunderagent. `/deep-research` startes ikke lenger automatisk.

### v2.1.218
`/code-review` kjører nå som en bakgrunnsunderagent. `/deep-research` startes bare når den påkalles manuelt.

### v2.1.217
`/resume` og `/continue` håndterer nå riktig malformerte vedleggsoppføringer. `/compact` fungerer korrekt for Claude Opus 4.8 på Bedrock.

### v2.1.215
Claude kjører ikke lenger `/verify` og `/code-review` skills automatisk. Påkall dem eksplisitt når det er nødvendig.

### v2.1.214
`/fork` redesignet for å kopiere samtaler til en ny bakgrunnsøkt mens du fortsetter å arbeide.

### v2.1.212
Ny `/fork`-atferd og `/resume`-velgerforbedriger.

### v2.1.207
`/cd`-kommandoen gir katalogveistøtte. `/doctor` utvidet til en full oppsettkontroll.

### v2.1.206
`/cd` gir nå katalogveistøtte. `/doctor` kan diagnostisere og fikse problemer, inkludert CLAUDE.md trimming.

### v2.1.205
`/doctor` er nå en full oppsettkontroll som kan diagnostisere og fikse problemer. Alias: `/checkup`.

## Nytt i v2.1.258

**Øktspesjifikke innsatsendringer (2.1.257):** Kommandoen `/effort` aksepterer nå `s` for å endre innsats for den gjeldende økten bare, som matcher oppførselen til `/model`.

## Nytt i v2.1.259

2.1.259 legger til `--json`-flagg til `claude plugin validate` for en maskinlesbar valideringsrapport. I tillegg forklarer `/install-github-app` nå at det er kun for GitHub og peker til GitLab CI/CD-dokumentasjonen når den kjøres inne i et GitLab-kodelager.

## Nytt i v2.1.260

Ny `/diff`-kommando: Åpner en diffpanel ved siden av samtalen i fullskjermmodus og viser dine ucommittede endringer når Claude redigerer kodebasen.

## Nytt i v2.1.261

Ny `/skill-doctor`-slash-kommando viser hvilke innlastede skills som ikke brukes og hva de koster i kontekst, slik at du kan fjerne unødvendige skills. Kommandoene `/status` og `claude doctor` inkluderer nå en "Organization policy"-linje som forklarer hvorfor organisasjonens policy ikke kunne lastes, for eksempel når en proxy ikke sender endepunktet gjennom.

## Nytt i v2.1.263

Lagt til i 2.1.260: Bruk /diff til å åpne et diff-panel som viser ucommittede endringer ved siden av samtalen i fullskjermmodus. Lagt til i 2.1.261: Bruk /skill-doctor for å se hvilke innlastede skills som ikke brukes og deres kontekstkostnad, slik at du kan fjerne unødvendige skills.
