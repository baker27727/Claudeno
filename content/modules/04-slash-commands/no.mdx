---
title: "Slash-kommandoer"
description: "Håndter kontekst, bytt modell, angre feil, og konfigurer økten uten å forlate samtalen."
---
import Callout from "../../../src/components/Callout.astro";

# Slash-kommandoer

Inne i en interaktiv økt styrer kommandoer som starter med `/` selve økten — konteksten
din, modellen din, historikken din — i stedet for å be Claude gjøre noe i koden din. En
kommando gjenkjennes bare i starten av meldingen din; alt etter blir et argument. Når du er i tvil, funker alltid denne:

```bash
/help
```

## Håndtere konteksten din

To kommandoer høres like ut, men gjør svært forskjellige ting.

```bash
/clear
```

`/clear` starter en helt ny samtale med fullstendig tom kontekst. Den gamle samtalen
er ikke borte — den er bare parkert; du kan åpne den igjen senere. Aliaser: `/reset`,
`/new`.

```bash
/compact
```

`/compact` frigjør plass ved å **oppsummere** samtalen så langt, i stedet for å tømme
den. Du fortsetter å jobbe i samme samtale, bare med et lettere kontekstvindu.

<Callout type="tip" title="Tips">
  Bruk `/clear` når du bytter til en helt urelatert oppgave. Bruk `/compact` når du
  fortsatt jobber med det samme, men samtalen har blitt lang.
</Callout>

## Fortsette senere

```bash
/resume
```

`/resume` åpner en tidligere samtale på nytt ved å velge den fra en liste, også de du
forlot med `/clear`. Alias: `/continue`. Dette er annerledes enn `-c`/`--continue` på
CLI-nivå (dekket i [CLI-grunnleggende](/no/learn/cli-basics)), som alltid gjenopptar
den *siste* samtalen uten en velger.

## Bytte modell midt i økten

```bash
/model
```

`/model` åpner en velger for å bytte hvilken Claude-modell økten bruker, og lagrer
valget som standard for nye økter. Ingen grunn til å restarte Claude Code for å prøve
en annen modell.

## Angre en feil

```bash
/rewind
```

`/rewind` går tilbake til et tidligere punkt — i koden, i samtalen, eller begge deler.
Det er det nærmeste du kommer et sikkerhetsnett når en endring gikk et sted du ikke
ville. Aliaser: `/checkpoint`, `/undo`.

<Callout type="mistake" title="Vanlig feil">
  Å bruke `/clear` for å "angre" en dårlig endring fjerner bare konteksten din — den
  rører ikke filene Claude endret. Bruk `/rewind` for faktisk å rulle tilbake kode.
</Callout>

## Flere kommandoer verdt å kjenne til

| Kommando | Formål |
|---|---|
| `/permissions` | Administrer allow/ask/deny-regler for verktøy. Alias `/allowed-tools`. |
| `/mcp` | Koble til, list opp, eller koble til på nytt MCP-tjenere. |
| `/init` | Generer en start-`CLAUDE.md` for et nytt prosjekt. |

## Oppsummering

- `/clear` tømmer konteksten og starter på nytt; `/compact` oppsummerer uten å tømme.
- `/resume` åpner enhver tidligere samtale på nytt via navn eller ID — ingenting er
  virkelig tapt.
- `/model` bytter modell uten å restarte økten.
- `/rewind` er den ekte "angre"-kommandoen for kodeendringer, ikke `/clear`.

## Relaterte skills

En skill oppfører seg som en slash-kommando som også kan utløse seg selv: `/navn` fungerer akkurat som en egendefinert kommando, men Claude laster den også automatisk når forespørselen din matcher beskrivelsen. Lær hvordan i [Skills og SKILL.md](/no/learn/skills/), eller [utforsk den kuraterte skills-katalogen](/no/skills/).

## Nytt i v2.1.205

/doctor er nå en fullstendig oppsettkontroll som kan diagnostisere og fikse problemer (i versjon 2.1.205). Bruk /checkup som alias. Merk at oppstartskvareringar som "claude command missing or broken" er nå flyttet fra oppstart til /doctor og /status-utdata.

## Nytt i v2.1.206

Kommandoen `/cd` tilbyr nå katalogveistøtte som ligner på atferden til `/add-dir`, noe som gjør det enklere å navigere til ofte brukte kataloger. I tillegg er `/doctor` (med alias `/checkup`) blitt utvidet til et fullt oppsettskontrollverktøy som kan diagnostisere og fikse problemer, inkludert en ny kontroll som foreslår å trimme innsjekket `CLAUDE.md`-filer ved å fjerne innhold som Claude kan utlede fra kodekallet. Kommandoen `/commit-push-pr` tillater nå automatisk `git push` til kodelajers konfigurerte push-fjernkilde (via `remote.pushDefault`, eller den eneste fjernkilden når bare én er konfigurert) i tillegg til `origin`. Kommandoen `/login` på Gateway støtter nå Anthropic-drivne offentlige gatewayslutpunkter.

## Nytt i v2.1.207

Kommandoen `/cd` gir nå forslag til katalogbaner som samsvarer med oppførselen til `/add-dir`. Kommandoen `/doctor` er utvidet til en full oppsettsjekk som kan diagnostisere og fikse problemer (alias: `/checkup`), inkludert en sjekk som foreslår å trimme innsjekket `CLAUDE.md`-filer ved å kutte innhold Claude kunne utlede fra kodelegemet.

## Nytt i v2.1.212

**Nytt `/fork`-atferd**: `/fork` kopierer nå samtalen din til en ny bakgrunnsøkt (synlig som sin egen rad i `claude agents`) mens du fortsetter å arbeide i gjeldende økt. Underagenten som tidligere ble startet i økten er nå tilgjengelig via `/subtask`. **Ny `/resume`-velger**: Skriv `/resume` i agentvisningen for å åpne en velger av tidligere økter—inkludert slettede—og gjenoppta valget ditt som en bakgrunnsøkt.

## Nytt i v2.1.214

`/fork`-kommandoen er blitt redesignet i versjon 2.1.212: den kopierer nå samtalen din til en ny bakgrunnsøkt (en ny rad i `claude agents`) mens du fortsetter å arbeide i den gjeldende økten. Den tidligere in-session underagent-startprogrammet er blitt erstattet av `/subtask`-kommandoen.

## Nytt i v2.1.215

Claude kjører ikke lenger `/verify` og `/code-review` skills automatisk. Påkall dem eksplisitt med `/verify` eller `/code-review` når du ønsker at de skal kjøre.

## Nytt i v2.1.217

Kommandoene `/resume` og `/continue` håndterer nå korrekt malformerte vedleggsoppføringer i utskrifter i stedet for å mislykkes med en TypeError. Kommandoen `/compact` fungerer korrekt for Claude Opus 4.8 på Bedrock og mislykkes ikke lenger når grensen overskrides.

## Nytt i v2.1.218

Kommandoen `/code-review` kjører nå som en underagent i bakgrunnen, slik at konversasjonen din forblir fokusert på annet arbeid mens gjennomganger behandles i bakgrunnen. Tilsvarende starter `/deep-research` nå bare når den påkalles manuelt i stedet for å starte automatisk. Kommandoen `/ultrareview` har forbedret feilbehandling for å hjelpe Claude med å korrigere ugyldige argumenter, og beskrivende argumenter som "review my auth changes" brukes nå som notater til funn på gjeldende gren.

## Nytt i v2.1.220

/code-review kjører nå som en bakgrunnsunderagent, og holder gjennomgangsarbeid utenfor hovedsamtalen. `/deep-research` startes ikke lenger automatisk; det kjører bare når du starter det manuelt. Dynamiske arbeidsflyter bruker nå en medium størrelsesretningslinje som standard (færre enn 15 agenter); juster via alternativet Dynamic workflow size i `/config`.

## Nytt i v2.1.221

Nytt i 2.1.221: Kommandoen `/plugin install` oppdaterer nå et foreldet markedsplatakatalog og prøver på nytt før den rapporterer at et plugin ikke ble funnet. I tillegg aktiveres plugin installert via `/plugin` nå umiddelbart når det er sikkert, i stedet for alltid å kreve `/reload-plugins`. Kommandoen `/status` er forbedret til å vise øktstypen: `interactive`, eller en bakgrunnsjobb som er `attached` eller `unattended`.

## Nytt i v2.1.223

Kommandoen `/review` er nå et alias for `/code-review`, som gjennomgår den nåværende diffen eller en pull-forespørsel (`/code-review <level> <pr#>`). Bruk `/code-review ultra` for en grundig sky-gjennomgang. Når du skriver et nivå som `/code-review high`, blir det ditt nye standardvalg for påfølgende gjennomganger i den økten.

## Nytt i v2.1.224

Nytt i 2.1.224: Når du fjerner utilgjengelige limeavsnitt-endringer som vil endre en kommandos tekst, inkluderer Claude Code nå et avbryt-og-bekreft-trinn for å forhindre utilsiktet endring.

## Nytt i v2.1.226

**Nytt i 2.1.226:** Kommandoen `/teleport` er tilgjengelig i sky-økter, og viser hvordan du fortsetter en økt lokalt ved hjelp av `claude --teleport <session id>`.

## Nytt i v2.1.227

**Nytt i v2.1.227:** Slash-kommandomenyen er forbedret – blå markerer nå bare den valgte raden, matchede tegn er uthevet i stedet for fargeendret, og emoji eller акцентerte navn beholder sine tegn. I tillegg gjenbruker `/code-review` (alias `/review`) nå ditt siste innsatsnivå når det kalles uten nivå, og du kan angi dybde som `/code-review ultra` for dype skytestudier.

## Nytt i v2.1.232

**Meldinger på tvers av økter (v2.1.232):** Skriv `@` i ledeteksten for å nevne en annen Claude-økt ved navn; Claude bruker deretter `SendMessage` for å nå økten direkte. `SendMessage` leverer nå til et navn som nøyaktig samsvarer med en aktiv økt, noe som eliminerer bekreftelsesteg. Bruk `/config` til å stille inn "Messages from your other sessions" (Meldinger fra dine andre økter) til å godta, holde eller avvise innkommende meldinger på tvers av økter.

## Nytt i v2.1.233

Nytt i 2.1.233: Velgeren `/effort` i skjermleser-modus gjengis nå som en nummerert liste med en tastet talletterspørsel for forbedret tilgjengelighet.

## Nytt i v2.1.234

`/config`-dialogen inkluderer nå "Fortsett automatisk ved bruksgrenseutløp" og "Dialogutløp"-innstillinger. Du kan også åpne `/permissions`, `/add-dir`, `/autocompact`, `/theme`, `/help`, `/config`, og `/advisor`-dialoger mens Claude jobber—regelendringer gjelder for resten av gjeldende sving.

## Nytt i v2.1.235

2.1.235 forbedrer `/permissions`: dialogvinduet kan nå åpnes mens Claude arbeider, og regelendringer gjelder resten av gjeldende tur.

## Nytt i v2.1.237

/config-kommandoen inkluderer nå en "Output style"-innstilling der du kan velge den nye innebygde "Concise"-stilen for å få resultatorientert utdata med minimal innledning.

## Nytt i v2.1.245

**Nytt i 2.1.243:** `/status` viser nå en `Skipped sources`-linje som viser administrerte instillingskilder (for eksempel `managed-settings.json`) som er tilstede, men ikke brukt fordi en høyere prioritet administrert kilde er aktiv. `/status` viser også om GitHub er tilkoblet for Claude Code på nettet (Pro/Max), og peker til `/web-setup` når det ikke er det. `/mcp` og `/plugins` viser en `managed`-merke på claude.ai-tilkoblinger hvis autentisering administreres av organisasjonen din.

## Nytt i v2.1.246

**Automatiseringstillatelser (nytt i 2.1.246):** Bruk det nye Automodus-fliken i `/permissions` for å vise og redigere automodus klassifisererregler direkte fra kommandograsjekgrensesnittet.
